# DEV environment — safe to experiment, uses dev/sandbox YNAB credentials
# Copy to .env to activate: cp .env.dev.example .env

ENVIRONMENT=dev
YNAB_API_KEY="your_dev_api_key_here"
LOG_LEVEL=debug
DEBUG_MODE=True
HTTP_PORT=3000
