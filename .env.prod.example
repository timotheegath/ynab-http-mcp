# PROD environment — real YNAB data, handle with care
# Copy to .env to activate: cp .env.prod.example .env
# Never run this from an active coding-agent session.

ENVIRONMENT=prod
YNAB_API_KEY="your_prod_api_key_here"
LOG_LEVEL=info
DEBUG_MODE=False
HTTP_PORT=3000
